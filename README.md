# unraid-provisioner
Unraid plugin for deploying "turnkey" servers based on **profiles** that specify the plugins and Docker containers to be installed manually or automatically when the array is first booted.
